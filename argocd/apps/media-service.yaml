apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: media-service
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/G11-Engineering/g11-k8s-charts.git
    targetRevision: HEAD
    path: charts/media-service
    helm:
      releaseName: media-service
  destination:
    server: https://kubernetes.default.svc
    namespace: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true
